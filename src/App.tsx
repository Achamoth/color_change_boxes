import "./App.css";
import React from "react";

interface BoxProps {
  text: string;
  color: string;
  width: number;
  length: number;
}

function Box({ text, color, width, length }: BoxProps): React.ReactElement {
  return;
}

export default function App() {
  return <div />;
}
